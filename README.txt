The project implemented in Python 3.6

randsentence.py class is the class for part I. 
It reads grammar from cfg.txt. 
Writes the generated sentences to random-sentence.txt

parse.py class is the class for part II. 
It reads grammar from cfg.txt. 
It reads the example sentences from sentence.txt. Prints the result sentences as an output. Results can be displayed from ide.